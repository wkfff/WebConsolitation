using Krista.FM.Domain;

namespace Krista.FM.RIA.Extensions.Informator
{
    public interface INewsService
    {
        int GetNewMessagesCount(int currentUserId);

        MessageView RecieveOneMessage(int id);

        bool UploadAttachment(MessageAttachment messageAttachment);

        string GetSubject(string subject);

        string GetBody(string body);
    }
}
