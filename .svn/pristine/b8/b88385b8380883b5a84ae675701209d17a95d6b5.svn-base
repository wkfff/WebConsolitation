using System;
using System.Collections.Generic;
using System.Text;

namespace Krista.FM.Common
{
	//
    public class CheckPath : CheckRule
    {
        public CheckPath(string parameter, string value, string errorMessage, bool invalid)
            : base(parameter, value, errorMessage, invalid)
        {
        }

        public override bool Execute(string value)
        {
            return base.Execute(value);
        }

    }
}
